# Reusable Studio Engine

- Preserve the one-signal, one-loop, one-form constraint unless the project charter changes.
- Keep browser code dependency-free and organized by responsibility.
- Before requesting code, write intent and constraints in `docs/`.
- After generated code, annotate what changed and why in `process/changelog.md`.
- Validate visible behavior in the browser before polishing.
